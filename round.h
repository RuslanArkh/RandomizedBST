#ifndef ROUND_H
#define ROUND_H

#include <QDebug>
#include <QWidget>
#include <QPainter>
#include <QGraphicsItem>
#include "widget.h"


//struct MazeField;


/*class MyRound : public QGraphicsItem
{
public:
    MyRound();
    //Widget * f;
    QRectF boundingRect() const;
    void paint(QPainter * painter,
               const QStyleOptionGraphicsItem *option,
               QWidget *widget);

protected:
    MazeField * f;
    void SendField(Widget::MazeField & c_pField);
    void keyPressEvent(QKeyEvent * event);
};*/

#endif // ROUND_H
